{
  "name": "nebulastudios-website",
  "version": "3.4.11",
  "private": true,
  "description": "TypeScript type-checking only; bundling is handled by esbuild via cmd/build.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
